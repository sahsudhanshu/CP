#include <bits/stdc++.h>
using namespace std;

int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		string str1, str2;
		cin >> str1 >> str2;
		if (str1.length() < str2.length())
		{
			cout << -1 << endl;
			continue;
		}
		for (char c2 : str2)
		{	
			for (char c1: str1)
			{
				
			}
			
		}
	}
}
