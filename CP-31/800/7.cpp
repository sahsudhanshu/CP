#include <iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, m;
        cin >> n >> m;
        char x[n], s[m];
        cin >> x >> s;

        
    }

    return 0;
}